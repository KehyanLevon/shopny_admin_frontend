import { Box, Typography } from "@mui/material";

export const LoginPage: React.FC = () => {
  return (
    <Box
      minHeight="100vh"
      display="flex"
      alignItems="center"
      justifyContent="center"
    >
      <Typography variant="h4">Admin Login (stub)</Typography>
    </Box>
  );
};
